# 🚀 PHP Simple: Micro-Framework Leve e Puro
> _Pensado para iniciantes em PHP que entendem o basico e pretendem organizar seus projetos (estudantes, etc.) não recomendado para 'coisas sérias'l_

**PHP Simple** é um boilerplate/micro-framework minimalista construído em PHP puro. Ideal para projetos web pequenos e médios que buscam uma estrutura organizada sem a complexidade e o overhead de grandes frameworks.

O objetivo é fornecer um **Front Controller**, roteamento intuitivo, autoloader de classes e um conjunto de *helpers* essenciais (Database, Layout e Assets) para que você comece a escrever a lógica do seu negócio imediatamente.

## ✨ Recursos Principais

* **Front Controller Simples:** Todas as requisições são direcionadas para `main.php`.
* **Roteamento Baseado em Arquivos:** Mapeia URLs diretamente para arquivos em `/pages/`.
* **Autoloader PSR-4 Básico:** Carregamento automático de classes na pasta `/backend/classes/`.
* **Helpers de Banco de Dados (PDO):** Funções simples para `db_fetch`, `db_execute`, etc., usando *Prepared Statements*.
* **Sistema de Layout:** Função `layout()` baseada em *Output Buffering* para fácil inclusão de Header e Footer.
* **Estrutura de Código Limpa:** Separação clara entre o Core (`backend/`), as Views/Controllers (`pages/`) e os Templates (`templates/`).

## 📁 Estrutura do Projeto
```
/
|-- assets/           # Arquivos estáticos (CSS, JS, Imagens, Fontes)
|-- backend/          # Núcleo e lógica do sistema
|   |-- configs/      # Arquivos de configuração (DB, etc.)
|   |-- classes/      # Classes OOP (autoloader ativo)
|   |-- functions/    # Funções auxiliares (helpers)
|   |-- init.php      # Arquivo de inicialização do sistema
|-- pages/            # Arquivos PHP que representam as páginas (Views/Controllers)
|-- templates/        # Componentes de layout (header, footer, snippets)
|-- .htaccess         # Configuração do reescrita de URL
|-- main.php          # O "Front Controller" do projeto
```

## 🛠️ Como Começar

### Pré-requisitos

* PHP 7.4+
* Servidor Web (Apache recomendado com `mod_rewrite` ativado)
* MySQL/MariaDB

### Instalação e Configuração

1.  **Clone o Repositório:**
    ```bash
    git clone [URL_DO_SEU_REPOSITORIO] php_simple
    ```

2.  **Configurar o Servidor:**
    * Certifique-se de que a pasta `php_simple` está acessível via URL (ex: `http://localhost/php_simple/`).
    * **Ajuste o `.htaccess`:** Se o nome da pasta for diferente de `php_simple` ou você estiver em um subdomínio, ajuste a linha `RewriteBase /php_simple/` no `.htaccess`.

3.  **Configurar o Banco de Dados:**
    * Edite o arquivo `backend/configs/database.php` com suas credenciais:
        ```php
        // backend/configs/database.php
        define('DB_HOST', 'localhost');
        define('DB_NAME', 'seu_banco');
        define('DB_USER', 'seu_usuario');
        define('DB_PASS', 'sua_senha');
        // ...
        ```

4.  **Ajustar `main.php`:**
    * Defina a `$baseUrl` e o parâmetro da função `routeToPage()` para o nome da subpasta onde o projeto está.
        ```php
        // main.php
        $baseUrl = "http://localhost/php_simple/";
        routeToPage('php_simple'); // <- Deve ser o nome da subpasta
        ```

## 📄 Documentação Completa

Para detalhes sobre o roteamento, uso de helpers e a estrutura de layout, consulte a [página de documentação](docs/).